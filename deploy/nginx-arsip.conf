# /etc/nginx/sites-available/sis-arsip
# Akses cepat via IP: http://IP_VPS:8090  (sebelum domain/SSL siap)
server {
    listen 8090;
    server_name _;

    client_max_body_size 25M;

    location /static/ {
        alias /var/www/sis-arsip/static/;
        expires 7d;
    }

    location / {
        proxy_pass http://127.0.0.1:5050;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 90;
    }
}

# ── Domain + HTTPS (aktifkan setelah DNS mengarah & certbot dijalankan) ──────
# 1) Arahkan A record arsip.bmtamalmuslim.web.id -> IP VPS
# 2) certbot --nginx -d arsip.bmtamalmuslim.web.id
# 3) Hapus komentar blok di bawah (atau biarkan certbot yang mengelola)
#
# server {
#     listen 80;
#     server_name arsip.bmtamalmuslim.web.id;
#     return 301 https://$host$request_uri;
# }
# server {
#     listen 443 ssl http2;
#     server_name arsip.bmtamalmuslim.web.id;
#     ssl_certificate     /etc/letsencrypt/live/arsip.bmtamalmuslim.web.id/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/arsip.bmtamalmuslim.web.id/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     client_max_body_size 25M;
#     location /static/ { alias /var/www/sis-arsip/static/; expires 30d; add_header Cache-Control "public, immutable"; }
#     location / {
#         proxy_pass http://127.0.0.1:5050;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 90;
#     }
# }
